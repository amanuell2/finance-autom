---
import financeIllustration from '../assets/finance-illustration.png';
---

<div class="welcome-screen">
  <div class="content">
    <h1 class="title">
      Pay <span class="highlight-green">bills</span>, 
      <span class="highlight-purple">rent</span>,
      <br/>or buy <span class="highlight-pink">groceries</span>.
    </h1>
    <p class="subtitle">
      We help hard-working employees<br/>
      get ahead on their finances.
    </p>
    <div class="illustration">
      <img src={financeIllustration.src} alt="Financial growth illustration" />
    </div>
    <a href="/dashboard" class="get-started">
      Get started
      <span class="arrow">→</span>
    </a>
  </div>
</div>

<style>
  .welcome-screen {
    background-color: #FDEFA6;
height: 100vh;
  }

  .content {
   display: flex;
   flex-direction: column;
   align-items: flex-start; 
   justify-content: stretch;
   min-height: calc(100vh - 4rem);
   padding: 2rem;
  }

  .title {
    font-size: 2.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    line-height: 1.5;
    font-family: var(--font-mona);
    letter-spacing: -0.03em;
  }

  .highlight-green {
    background-color: #D1FFD1;
    padding: 0.2rem 0.5rem;
    border-radius: 1rem;
  }

  .highlight-purple {
    background-color: #E6E6FF;
    padding: 0.2rem 0.5rem;
    border-radius: 1rem;
  }

  .highlight-pink {
    background-color: #FFD6E8;
    padding: 0.2rem 0.5rem;
    border-radius: 1rem;
  }

  .subtitle {
    color: #666;
    font-size: 1.1rem;
    margin-bottom: 2rem;
    font-family: var(--font-mona);
    font-weight: 400;
  }

  .illustration {
  margin-bottom: auto;
  margin-top: auto;
  }

  .illustration img {
    width: 100%;
    height: auto;
  }

  .get-started {
    background-color: #000;
    color: white;
    text-decoration: none;
    padding: 1rem 2rem;
    border-radius: 2rem;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.2s ease;
    align-self: center;
    font-family: var(--font-mona);
    letter-spacing: -0.01em;
  }

  .get-started:hover {
    transform: scale(1.05);
    background-color: #333;
  }

  .get-started:active {
    transform: scale(0.98);
  }

  .arrow {
    font-size: 1.2rem;
  }
</style>
